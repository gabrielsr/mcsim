{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug run.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/demo/run.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}